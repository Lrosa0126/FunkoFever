const express = require('express');
const router = express.Router();

// Define the route handler for the home route
router.get('/', (req, res) => {
  // Handle the home route logic
  res.send('Hello, this is the home page');
});

// Export the router
module.exports = router;
